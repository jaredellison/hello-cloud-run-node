// eslint-disable-next-line no-undef
module.exports = {
  // Supports all esbuild.build options
  esbuild: {
    minify: false,
    sourcemap: true,
    keepNames: true,
    sourcesContent: false,
  },
};
